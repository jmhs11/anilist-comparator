export const validateUser = name => {
	let error;

	if (!name) return 'El usuario no puede estar vacío';

	// if (name.length < 3) return 'El usuario debe tener al menos 3 caracteres';

	return error;
};
